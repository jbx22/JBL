# JBL BIZ LAW — Drizzle Migrations
#
# Run: npx drizzle-kit generate (then review the SQL in ./drizzle/)
# Push to Neon DB: npx drizzle-kit push
#
# The schema is declared in: ./src/db/schema.ts
#
# Migration tracking uses Drizzle Kit's standard ./drizzle directory.
